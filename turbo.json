{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "inputs": ["**/*"],
      "outputs": ["**/public", ".vercel/**"]
    },
    "start": {
      "cache": false,
      "persistent": true
    },
    "clean": {}
  }
}
